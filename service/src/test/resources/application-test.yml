spring:
  datasource:
    username:
    password:
    url: jdbc:h2:mem:iddev;MODE=LEGACY
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate.format_sql: true